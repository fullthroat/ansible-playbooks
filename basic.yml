---
  - name: basic
    hosts: localhost
    become: yes
    become_user: root
    tasks:
      - name: ensure apache is at the latest version
        apt:
          name: apache
          state: latest
      - name: ensure apache is running
        service:
          name: httpd
          state: started
